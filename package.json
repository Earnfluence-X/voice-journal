{
  "name": "voice-journal",
  "version": "1.0.0",
  "description": "Voice Journal with Vercel backend",
  "main": "index.html",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod",
    "build": "echo 'No build step required'"
  },
  "dependencies": {
    "node-fetch": "^2.6.7"
  },
  "devDependencies": {
    "vercel": "^34.0.0"
  }
}